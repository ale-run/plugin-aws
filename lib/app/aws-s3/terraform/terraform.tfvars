#environment            = "dev"
region                  = "${region}"
#only lowercase letters, numbers, dots(.), and hyphens(-). 3(min) and 63(max) characters long
bucket_name             = "${identifier}"
